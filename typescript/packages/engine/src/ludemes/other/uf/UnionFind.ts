// @java Core/src/other/uf/UnionFind.java UnionFind
/**
 * Main union-find (one-by-one move) entry point.
 *
 * Faithful 1:1 transliteration of other.uf.UnionFind.
 *
 * NOTE: the Java class contains almost entirely commented-out methods.  The
 * only live code is the private constructor.  This transliteration mirrors
 * that exactly: a utility class with a private constructor and no public
 * methods (the commented-out implementations are preserved as TS comments).
 *
 * @author tahmina  (Java original)
 */

/**
 * Union-find entry point (one-by-one-move variant).
 * @java other.uf.UnionFind
 */
export class UnionFind {
  /** Utility class — private constructor. @java UnionFind() */
  private constructor() { /* no-op */ }
}

/*
 * NOTE: The bulk of the Java class (eval, union, connected, find …) is
 * commented out in the source.  The structure above faithfully mirrors that:
 * an otherwise-empty utility class.  If those methods are ever uncommented in
 * the Java source they should be transliterated here as well.
 */
