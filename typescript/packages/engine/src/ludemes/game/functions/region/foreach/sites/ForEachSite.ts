// @java Core/src/game/functions/region/foreach/sites/ForEachSite.java

// TODO: constructor-arm class, not directly dispatchable by the current registry.
// The outer (forEach <region> if:...) port is in ../ForEach.ts, but the root is
// not registered until Team context support exists (ForEachSite.java:55-72).
