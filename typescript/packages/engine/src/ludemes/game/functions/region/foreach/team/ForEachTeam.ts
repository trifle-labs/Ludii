// @java Core/src/game/functions/region/foreach/team/ForEachTeam.java

// TODO: constructor-arm class, not directly dispatchable by the current registry.
// The outer (forEach Team ...) port is in ../ForEach.ts, but EvalFrame lacks the
// full context.team array that ForEachTeam.eval writes (ForEachTeam.java:43-68).
