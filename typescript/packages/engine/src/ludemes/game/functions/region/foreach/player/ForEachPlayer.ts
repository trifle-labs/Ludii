// @java Core/src/game/functions/region/foreach/player/ForEachPlayer.java

// TODO: constructor-arm class, not directly dispatchable by the current registry.
// The outer (forEach (players ...) ...) port is in ../ForEach.ts, but the root
// is not registered until Team context support exists (ForEachPlayer.java:53-90).
