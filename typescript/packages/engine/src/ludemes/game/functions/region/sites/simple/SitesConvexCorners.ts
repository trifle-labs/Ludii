// @java Core/src/game/functions/region/sites/simple/SitesConvexCorners.java

// TODO: needs faithful topology CORNER_CONVEX property exposure. Java
// SitesConvexCorners.eval delegates directly to graph.cornersConvex(realType)
// (SitesConvexCorners.java:44-55); the TS graph facade currently exposes only
// the combined corner set, not the convex/concave split.
