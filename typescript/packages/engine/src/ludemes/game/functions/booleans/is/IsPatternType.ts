// @java Core/src/game/functions/booleans/is/IsPatternType.java

/**
 * Defines the types of Is for the detection of a pattern.
 *
 * @author Eric.Piette
 */
export enum IsPatternType {
  /** To detect a pattern from a site. */
  Pattern = 0,

  /** To detect a pyramid corners from a site. */
  PyramidCorners = 1,
}
