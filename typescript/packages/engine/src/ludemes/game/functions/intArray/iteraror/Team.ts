// @java Core/src/game/functions/intArray/iteraror/Team.java

// STUB: Team.eval returns `context.team()` (Team.java:32-36), but EvalFrame has
// no team slot and the existing `(forEach Team ...)` port is intentionally
// blocked for the same reason. Needs EvalFrame.team support before `(team)` can
// be registered faithfully.
