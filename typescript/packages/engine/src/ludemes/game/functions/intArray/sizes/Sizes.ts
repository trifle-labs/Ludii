// @java Core/src/game/functions/intArray/sizes/Sizes.java

// STUB: Sizes.construct only dispatches `(sizes Group ...)` to SizesGroup
// (Sizes.java:44-77), but a faithful SizesGroup port needs Java's 3D visibility
// checks for `isVisible:True` (SizesGroup.java:152-160, 185-241) in addition to
// the ordinary connected-group traversal (SizesGroup.java:103-270). The current
// TS topology/state surface does not expose containerId/upward coverage, so this
// root ludeme remains unregistered and falls back to compile.ts for now.
