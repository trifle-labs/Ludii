// @java Core/src/game/functions/intArray/state/Rotations.java

// STUB: faithful Rotations needs Java's topology-supported DirectionFacing
// surface: topology.numEdges(), supportedDirections(defaultSite), relation to
// facing conversion, and DirectionFacing.index() (Rotations.java:63-106). The
// current TS direction API exposes step offsets, not those rotation indices, so
// registering `(rotations ...)` would be an approximation.
