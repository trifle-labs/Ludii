/**
 * Defines properties that can be set in the starting rules that require only
 * the graph element type.
 *
 * @java game/rules/start/set/SetStartGraphType.java — enum SetStartGraphType
 */
export enum SetStartGraphType {
  /** Makes all items invisible to all players. */
  AllInvisible = "AllInvisible",
}
