// @java Core/src/game/rules/play/moves/nonDecision/operators/foreach/ForEachDirectionType.java

/**
 * Defines the direction which can be iterated in the ForEach super ludeme.
 *
 * @java game/rules/play/moves/nonDecision/operators/foreach/ForEachDirectionType.java
 * @author Eric.Piette
 */
export enum ForEachDirectionType {
  /**
   * To apply a move for each site reached according to a direction.
   */
  Direction = "Direction",
}
