/**
 * @java metadata/MetadataItem.java MetadataItem
 *
 * Marker interface for generic metadata items.
 * Classes included as metadata should implement this interface.
 *
 * @author cambolbro
 */

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface MetadataItem {
  // Nothing to add — this interface is just for identifying metadata items.
}
